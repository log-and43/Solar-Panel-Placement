# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.pytest_cache/

# Node
node_modules/
dist/
.vite/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local env
.env
.env.local

# Phase 2: raw downloads aren't committed; the built Parquet files ARE.
backend/data/raw/

# Phase 3: PVWatts cache accumulates locally; not committed
backend/data/pvwatts_cache.json

# Phase 4: polygon cache accumulates locally; not committed
backend/data/polygon_cache/

# Phase 5: finance Parquet files ARE committed (like the other data/*.parquet);
# only the raw download is ignored (already covered by data/raw/ above).
backend/data/grid_solar_cache.parquet
